# Feature Spec: <FEATURE NAME>

> Status: DRAFT
> Source of truth: Far Manager (`../FarManager/far/<file>.cpp`)
> Generated by the `far-feature-spec` skill on <DATE>
> Captures: `specs/<slug>.captures/`

<!--
  Fill EVERY section. If something genuinely does not apply write "N/A".
  If something could not be determined write "UNKNOWN - needs verification"
  and add it to section 0. Never leave a section blank.

  MANDATORY COVERAGE - sections 6 (conflicts), 7 (filesystem errors),
  8 (archive context), 9 (F1 help) and the focus/Esc/Enter/mouse detail in
  section 4 are required for every feature. If a feature truly has no archive
  behaviour or no filesystem errors, state that explicitly with evidence.
-->

## 0. Summary & open questions

- **Effect:** <one paragraph - what the feature does for the user>.
- **Trigger:** <key / menu path / mouse> in <context>.
- **Open questions / ASSUMED items:** <bullets - anything not verified against
  a capture or a source line>.

## 1. Far source references

| Concern | File | Lines |
|---|---|---|
| Implementation | `../FarManager/far/<x>.cpp` | |
| Dialog item array | `../FarManager/far/<x>.cpp` | |
| Dialog procedure (`*DlgProc`) | `../FarManager/far/<x>.cpp` | |
| Trigger key handling | `../FarManager/far/filelist.cpp` / `keybar.cpp` | |
| Archive path | `../FarManager/far/<x>.cpp` / plugin API | |
| Help topic | `../FarManager/far/FarEng.hlf.m4` `@<Topic>` | |
| Midnight Commander equivalent | `../mc/src/<x>.c` | |

## 2. Activation & trigger

- **Trigger:** <key>, available when <panel focused / dialog open / ...>.
- **Key table:**

| Key | Modifier | Context | Action |
|---|---|---|---|
| | | | |

- **Letter hotkeys (`&` accelerators):** <control -> letter, ...>.

## 3. UI elements

- **Dialog/overlay size:** <W>x<H> cells, <centred / anchored>.
- **Element table** - record colours for BOTH the unfocused and focused state
  (the focus highlight is part of the spec):

| # | Type (`DI_*`) | Position X1,Y1-X2,Y2 | Flags (`DIF_*`) | Caption | Far colour (`COL_*`) | Observed fg/bg unfocused | Observed fg/bg focused | VSCommander colour element |
|---|---|---|---|---|---|---|---|---|
| | | | | | | | | |

- **Captured states** (embed each `*.ansi.txt` grid in a fenced block; link
  the matching PNG). One capture per distinct visual state.

### State: <name>
```
<paste specs/<slug>.captures/<name>.ansi.txt here>
```
PNG: `specs/<slug>.captures/<name>.png`

## 4. Focus, keyboard & mouse behaviour

- **Focus cycle (Tab / Shift+Tab):** the exact order controls receive focus,
  whether it wraps (the "tab circle"), and **how the focused control changes
  colour** (cite the focused-state colours from section 3 and a capture).
- **Arrow keys / Space within a control:** <combobox cycling, checkbox toggle,
  edit-field caret movement, etc.>.
- **Enter:** behaviour from each control - the default button
  (`DIF_DEFAULTBUTTON`) and any control-specific Enter handling.
- **Escape:** what it does (close / cancel / pop one level).
- **Mouse behaviour** - per element:

| Target | Left click | Double-click | Wheel | Drag |
|---|---|---|---|---|
| | | | | |

## 5. Behaviour, defaults & effect

- **Default field values:** <initial edit text, default combo item, checkbox
  states - each with its source>.
- **Operation sequence on confirm:** <step 1 -> step 2 -> ...>.
- **Dynamic UI rules** (from the `*DlgProc`): <enable/disable, show/hide>.
- **Post-action panel state:** <cursor target, refresh, selection handling>.
- **Persistence:** <history / config remembered vs. in-memory only>.

## 6. Conflict handling (MANDATORY)

How the feature reacts when the target already exists or otherwise collides:
name already exists (as file / as directory), case-only differences, an
existing item of a different type. For each: what dialog/prompt appears, its
buttons, and the resulting behaviour. Cite a capture of each conflict state.

## 7. Filesystem error handling (MANDATORY)

For every failure class the feature can hit, record the error dialog (title,
message, buttons - typically Retry / Skip / Cancel) and behaviour:
permission denied, path not found, no space, read-only filesystem, name too
long, invalid characters, resource busy / locked, I/O error. Cite captures
and the source error path. State explicitly if a class cannot occur.

## 8. Archive context (MANDATORY)

How the feature behaves when the active panel is **inside an archive**
(ZIP / TAR / 7z / RAR): is it allowed, what differs, what happens on
read-only archive formats. State "N/A - feature not available in archives"
with evidence if it does not apply.

## 9. F1 help (MANDATORY)

Pressing `F1` in the feature's dialog/context. Record the Far help topic that
opens (`@Topic`), summarise its content, and note how VSCommander's own F1
help (`docs/`) must document this feature. Cite a capture of the help screen.

## 10. Edge cases (verified against Far)

| # | Edge case | Far behaviour | Evidence | Status |
|---|---|---|---|---|
| | | | capture `<name>` / `<file>:<line>` | VERIFIED / ASSUMED |

For filesystem features cover at least: name collision, missing permissions,
cross-filesystem / cross-drive, symlinks, hardlinks, recursive symlink loops,
fs errors, file in use, read-only/hidden/system attributes, long paths,
trailing separators, empty input, invalid characters, intermediate-directory
creation, case-only rename, network paths, concurrent modification.

## 11. Midnight Commander cross-reference

<Where MC differs from Far. Far governs; these are notes only.>

## 12. VSCommander implementation mapping

| Concern | Module (per `CLAUDE.md` architecture) |
|---|---|
| Drawing primitives | `src/draw.ts` |
| Popup/dialog | `src/<feature>Popup.ts` |
| Panel routing | `src/panel.ts` |
| Filesystem operations | `src/fileOps.ts` |
| VS Code API integration | `src/extension.ts` |
| Reusable controls | `inputControl.ts` / `buttonGroup.ts` / `popup.ts` / `formView.ts` |

- **Constraint:** no new file may import `vscode` (only `extension.ts` and
  `directoryInfo.ts` may).
- **Existing implementation in VSCommander:** <files, or "none"> - if it
  exists this spec is a delta/audit; list what matches and what is missing.
- **New theme elements required:** <list, or "none">.

### Intentional improvements over Far

VSCommander sometimes deliberately improves on Far Manager. List each
intentional improvement here, with the reason, and describe exactly how the
VSCommander behaviour differs from Far. These are deliberate deviations -
mark them clearly so they are never mistaken for bugs. If there are none,
write "none - replicate Far exactly".

| Improvement | Far behaviour | VSCommander behaviour | Reason |
|---|---|---|---|

### Divergences needing a decision

Where the **existing** VSCommander implementation already differs from Far and
it is NOT a documented intentional improvement above, list each divergence.
Do NOT silently pick one - the implementer must ASK the user which behaviour
to adopt (replicate Far / keep current VSCommander / a new improvement)
before proceeding.

| Divergence | Far behaviour | Current VSCommander behaviour | Decision (ASK) |
|---|---|---|---|

## 13. Documentation deliverables

- `docs/<Feature>.md` - H1 title + H2 sections, backtick hotkeys,
  `[links](File.md)`.
- `docs/Contents.md` - index entry.
- `USER.md` - corresponding section.
- `README.md` - note if a major feature.

## 14. Verification / test plan (MANDATORY - methodical, full coverage)

Every item in this spec must map to a test. Build the test list as a table so
nothing is skipped:

| Spec item | Test (`src/test/suite/<NNN> - ...`) | Reference screenshot(s) | Filesystem assertion |
|---|---|---|---|

Required coverage:
- Every keybinding and the trigger.
- Every distinct **UI state** -> its own reference screenshot
  (`expectScreenshot`): initial dialog, each focused control in the Tab cycle,
  combobox/checkbox toggled, after-confirm, after-cancel.
- Every **conflict** path (section 6).
- Every reproducible **filesystem error** (section 7).
- **Archive context** (section 8) if applicable.
- **F1 help** opening (section 9).
- Test directory layout, fixture (`filesystem/panel1`, `panel2`), and the
  key/mouse sequence for each test.

Reference screenshots are captured via the VSCommander harness
(`UPDATE_SCREENSHOTS=1 npm test`) - one per distinct visual state, named for
the state.
